<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">@color/accent</item>

        <item name="vpiCirclePageIndicatorStyle">@style/CustomCirclePageIndicator</item>
    </style>

    <style name="AppDialogTheme" parent="Theme.AppCompat.Light.Dialog">
    </style>

    <style name="CustomCirclePageIndicator">
        <item name="fillColor">@color/accent</item>
        <item name="strokeColor">@color/primary</item>
        <item name="strokeWidth">1dp</item>
        <item name="radius">8dp</item>
        <item name="centered">true</item>
    </style>

    <color name="primary">#669801</color>
    <color name="primary_dark">#3B5A01</color>
    <color name="accent">#0072AF</color>
    <color name="background">#ff2a394e</color>
    <color name="light_text">#696969</color>
    <color name="lighter_text">#bbbbbb</color>
</resources>
